#include "main.h"

/**
  * mul - multiple two number
  * @a: first input
  * @b: second input
  * Return: the multiple of the two number
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
